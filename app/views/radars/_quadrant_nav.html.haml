%ul.nav.nav-pills
  - QuadrantList.new.quadrants.each do |quadrant|
    %li{class: quadrant.value == params[:quadrant] ? "active" : ""}
      = link_to radar_quadrant_path(@radar, quadrant: quadrant.value) do
        = quadrant.name
        .badge= @radar.blips.in_quadrant(quadrant).count
