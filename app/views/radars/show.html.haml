.clearfix
  = title radar.name

%p= radar.description if radar.description?

%h3= "Created by #{radar.owner.name}" unless current_user

= render 'radars/management', radar: radar
%br
%br

= render 'radars/radar', radar: radar, svg: svg
