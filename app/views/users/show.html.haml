= title @user.username

= "#{@user.name} has published #{pluralize @user.radars.count, "radar"} with #{pluralize @user.blips.count, "blip"}."

%h2 Radars

%ul
- @user.radars.each do |radar|
  %li
    =link_to radar.name, radar
    (#{pluralize(radar.blips.count, 'blip')})
